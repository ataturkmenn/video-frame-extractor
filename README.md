# 🎥 Video Frame Extractor (Python + OpenCV)

This project extracts all frames from a video file and saves them as individual JPG images.

## 🚀 Features
- Extracts every frame from a video
- Automatically creates output folder
- Saves frames in sequential format (frame_0001.jpg)

## 🛠 Requirements
- Python 3.x
- OpenCV

Install dependencies:

```bash
pip install -r requirements.txt